prompt prompt
    match /[\/a-zA-Z0-9._:-]+ ?(?:\(config[^)]*\))? ?[#>] ?$/

prompt basic
    match /: ?$/

prompt user
    match /login as: ?$/

macro enable_paging
    page

macro disable_paging
    no page
